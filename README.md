# AWS-Terraaform-Ansible
Terraform scripts to provision AWS infra and Ansible playbook to download/install Wordpress

$ terraform init 

$ terraform apply -var-file="secret.tfvars" 

$ terraform plan

$ terraform apply
